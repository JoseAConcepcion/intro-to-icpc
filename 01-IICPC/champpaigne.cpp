#include <bits/stdc++.h>
using namespace std;
void solve()
{
    int n, q, level;
	cin >> n;
	cin >> q;
    int levels[n+1];
    for (int i = 1; i < n; i++)
    {
        cin >> levels[i];
    }


}
int main(){
    solve();
}